# Optional. The Setup tab UI is the preferred way to configure these — that
# stores them in config.yaml, and its value overrides these env vars when set.
# Create a TMDB API Read Access Token at https://www.themoviedb.org/settings/api
TMDB_API_TOKEN=
TMDB_LANGUAGE=en-US