__pycache__/
*.py[cod]
.venv/
.env
.env.*
!.env.example
build/
*.deb

# Live user config \u2014 ignored so `git pull` never wipes scan_paths, sliders,
# sweep time, etc. Template lives at config/config.yaml.example.
config/config.yaml
